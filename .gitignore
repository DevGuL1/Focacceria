# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Build and cache
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Session / local notes
.copilot/

# Large local-only uploads
media/home-video/

# Placeholder local files
Example txt file.txt
Another example txt file.txt

# NOTE: For Vercel we DO commit the bundled SQLite DB and collected static,
# because Vercel's read-only lambda bundles the repo (no build-time collectstatic).
# The DB is copied to /tmp at runtime for the demo; use DATABASE_URL for real persistence.
*.sqlite3

# Local env
.env
